# 01_project.md — 타로 기능 전체 프로젝트 설계

> 천지인 사주 웹앱(`index.html` 단일 SPA)에 추가되는 **몰입형 타로 리딩** 신규 카테고리의 마스터 설계 문서.
> 이후 작성될 `02_tarot_database.md` ~ `06_prompt_examples.md`는 모두 이 문서를 기준으로 확장한다.

---

## 0. 문서 지위와 참고 자료

| 자료 | 역할 |
|---|---|
| `tarot design/DESIGN.md` | 타로 전용 디자인 시스템 원본 (Celestial Arcana — 색·타이포·글래스모피즘 규칙) |
| `tarot design/code1.html` | 스프레드 선택 화면 시안 (3/5/10장 상품 카드 + 스프레드 미리보기) |
| `tarot design/code2.html` | 질문 입력 화면 시안 (질문 textarea + 분야 칩 + 글자수 카운터) |
| `tarot design/code3.html` | 카드 선택 화면 시안 (상단 슬롯 + 하단 78장 부채꼴 덱, 스크롤 포커스 로직 포함) |
| `tarot design/screen1~3.png` | 위 세 화면의 완성 렌더링 |
| 기존 `index.html` | 결제·잠금·i18n·저장 등 모든 인프라 컨벤션의 원본 |

**디자인 원칙 확정**: 시안의 색·레이아웃·글로우·글래스 패널은 따르되, **글자체는 기존 앱 그대로 유지한다**
(본문 `'Do Hyeon','Manrope','Noto Sans KR'` / 제목 `'Do Hyeon','Noto Serif KR'`). 시안의 EB Garamond·Hanken Grotesk 지정은 채택하지 않고 기존 폰트 스택으로 치환한다.
기존 앱의 `:root` 변수(`--bg:#111415`, `--accent-bright:#d4af37`, `--accent:#d8c4a8` 등)가 시안 팔레트와 사실상 동일하므로 **기존 변수를 그대로 쓰고, 타로 전용 변수 2개만 추가**한다 (→ §10).

---

## 1. 프로젝트 목적

기존 사주앱 안에 들어가는 신규 카테고리 "타로". 단순 랜덤 운세가 아니라 사용자가 **직접 카드를 고르고**, 질문에 맞는 **유료 리딩**을 받는 몰입형 경험을 만든다.

타로 메뉴는 2개로 구성한다.

1. **오늘의 타로** — 무료. 유입·재방문용. API 비용 0원.
2. **질문형 타로** — 유료 핵심 상품. 3장/5장/10장 리딩.

기존 앱과의 관계:
- 메인 메뉴 그리드(`menu-card`)에 타로 진입 카드 추가
- 결제는 기존 카카오페이 워커(`worker/kakao-payment-worker.js`)의 `CATEGORY_PRICE` 확장으로 처리 (초기엔 mock, → §11)
- AI 리딩은 기존 `worker/saju-chat-worker.js`와 같은 패턴의 워커 엔드포인트로 처리하되, 실패 시 로컬 템플릿 폴백 필수

---

## 2. 서비스 핵심 철학

단순히 카드를 랜덤으로 보여주는 앱이 아니다. 반드시 아래 감각을 살린다.

- 사용자가 **직접 카드를 고르는** 느낌 — 랜덤 배정 금지, 덱에서 손으로 뽑는 UX
- **질문에 맞는 리딩**을 받는 느낌 — 질문 분석 → 스프레드 추천 → 질문이 결과문 전체를 관통
- 결제 후 **충분히 깊은 풀이**를 받는 느낌 — 카드 나열이 아닌 하나의 상담문
- 기존 사주앱과 **자연스럽게 이어지는** 느낌 — 같은 어두운 배경, 같은 골드 포인트, 같은 말투(평이체)
- 장난스러운 운세가 아니라 **전문 상담**을 받는 느낌

톤앤매너는 기존 사주 풀이와 동일하게 **평이체**(한자·괄호 최소화, 짧은 문장, 일상어)를 쓴다.

---

## 3. 전체 메뉴 구조

```text
타로
├ 오늘의 타로                        (무료)
└ 질문형 타로                        (유료)
   ├ 질문 입력                       ← code2.html 시안
   ├ 리딩 상품 추천 + 선택           ← code1.html 시안
   ├ 결제                            (mock → 카카오페이)
   ├ 카드 선택                       ← code3.html 시안
   └ 결과 확인                       (저장·재열람 가능)
```

기존 SPA의 뷰 체계를 따른다. 카테고리 키는 다음으로 확정한다.

| 키 | 의미 | 과금 |
|---|---|---|
| `tarot-daily` | 오늘의 타로 | 무료 |
| `tarot3` | 질문형 3장 리딩 | 1,500원 |
| `tarot5` | 질문형 5장 리딩 | 2,000원 |
| `tarot10` | 질문형 10장 켈틱크로스 | 3,000원 |

---

## 4. 오늘의 타로 (무료 · API 없음)

### 4.1 흐름

```text
오늘의 타로 진입
→ 9장의 카드 뒷면 표시 (3×3 그리드)
→ 사용자가 1장 선택
→ 카드 뒤집기 애니메이션 (CSS 3D flip, 0.6s)
→ 오늘의 운세 출력
```

### 4.2 결과 항목

- 선택한 카드 (카드 비주얼)
- 카드명 (한글 + 원어 병기, 예: "연인 — The Lovers")
- 오늘의 전체운
- 연애운
- 재물운
- 일/직장운
- 오늘의 조언
- 주의할 점

### 4.3 생성 로직 (OpenAI API 사용 금지)

- 카드 데이터(`02_tarot_database.md`에서 정의)의 카드별 고정 텍스트 + 카테고리별 템플릿 문장 조합만으로 결과를 만든다.
- **시드 규칙**: `seed = hash(YYYY-MM-DD + userId)` — 기존 `_getOrCreateUserId()`를 재사용한다.
  - 9장 후보 카드 구성과 각 카드의 결과 텍스트 변주가 이 시드로 결정된다.
  - 같은 날 재진입하면 **같은 후보·같은 결과**가 나와야 한다 (신뢰감). 자정 지나면 갱신.
- 결과는 `localStorage`에 당일 캐시: `tarot_daily_v1:{userId}:{YYYY-MM-DD}` → 재진입 시 뽑았던 카드와 결과를 즉시 복원.
- 오늘의 타로는 **정방향만** 사용한다 (무료 기능 단순화).

### 4.4 전환 훅

결과 하단에 기존 `conversion-hook` 컴포넌트 패턴으로 질문형 타로 유도 섹션 1개를 배치한다.
문구 예: "오늘의 카드가 마음에 걸린다면, 지금 그 고민을 카드에게 직접 물어보세요." → `tarot` 질문 입력으로 이동.

---

## 5. 질문형 타로 (유료)

### 5.1 흐름

```text
질문 입력 (10~200자, 분야 칩 선택 가능)
→ 질문 분석 (로컬 키워드 분석, API 없음)
→ 3장 / 5장 / 10장 리딩 추천 (추천 배지 + 추천 사유 문구)
→ 상품 선택 (추천과 다른 상품 선택 가능)
→ 결제 (mock 구조, 이후 카카오페이 연결)
→ 카드 선택 화면 (78장 부채꼴 덱)
→ 카드 직접 선택 (스프레드 장수만큼)
→ 선택 완료
→ 결과 생성 (AI 워커 호출, 실패 시 로컬 템플릿)
→ 결과 저장 (재열람 가능)
```

### 5.2 상태 규칙

- 질문·선택 상품·선택 카드는 결제 왕복(카카오페이 앱 이동) 중에도 유실되면 안 된다.
  기존 결제 플로우의 `_savePendingPayment()` 패턴(sessionStorage + localStorage 이중 백업)을 그대로 따른다.
- 카드 선택 도중 이탈 후 재진입하면 "이어서 선택" 또는 "처음부터"를 물어본다.
- 결제 완료 후 24시간 내 재진입 시 결과 재열람 무료 (기존 24h 토큰 정책과 동일 UX).

---

## 6. 유료 상품 구성

가격은 시안(screen1.png)과 동일하게 확정한다.

```text
3장 리딩         : 1,500원   (tarot3)
5장 심층 분석     : 2,000원   (tarot5)  ← 기본 추천 상품
10장 켈틱크로스   : 3,000원   (tarot10)
```

### 6.1 — 3장 리딩

**용도**: 간단한 질문 · 연락 가능성 · 오늘/이번 주 흐름 · YES/NO 성격 질문 · 빠른 조언

**기본 배열**: `과거 / 현재 / 미래`

질문 성격에 따라 아래 배열로 자동 대체 가능 (엔진이 질문 분석 결과로 결정):

```text
나 / 상대 / 결과            (관계 질문)
문제 / 조언 / 결과          (해결형 질문)
현재 / 장애물 / 가능성      (가능성 질문)
```

### 6.2 — 5장 리딩

**용도**: 연애 · 재회 · 직장 · 금전 · 관계 · 선택 고민

**기본 배열**: `현재 상황 / 장애물 / 숨겨진 흐름 / 조언 / 결과`

레이아웃은 십자형 (screen3.png 기준: 중앙1, 상2, 하3, 좌4, 우5).

### 6.3 — 10장 켈틱크로스

**용도**: 장기적인 고민 · 복잡한 관계 · 인생 방향 · 사업 · 결혼 · 큰 선택

**기본 배열**:

```text
1 현재 상황      2 장애물        3 과거          4 가까운 미래    5 의식
6 무의식         7 본인의 태도    8 주변 환경      9 희망과 두려움  10 최종 결과
```

레이아웃은 전통 켈틱크로스 (좌측 십자 6장 + 우측 세로 스태프 4장, code1.html `layout-10` 참고).

---

## 7. 질문 분석 및 추천 로직 (로컬, API 없음)

`tarot/tarot-recommend.js` 유틸 함수로 분리한다. 키워드 매칭 점수제:
각 그룹에서 매칭된 키워드 개수 × 그룹 가중치로 점수를 내고 최고점 스프레드를 추천한다.
동점이거나 아무 키워드도 없으면 **5장을 추천**한다 (기본 추천 상품).

### 3장 추천 키워드

```text
오늘, 연락, 올까, 가능성, 해야 할까, 말까, YES, NO, 짧게, 지금, 현재, 이번 주
```

추천 문구:

```text
이 질문은 현재 상황과 가까운 흐름을 빠르게 보는 질문에 가깝습니다. 3장 리딩을 추천드립니다.
```

### 5장 추천 키워드

```text
연애, 재회, 상대방, 속마음, 직장, 이직, 금전, 돈, 고민, 선택, 관계, 앞으로
```

추천 문구:

```text
이 질문은 현재 상황, 장애물, 조언, 결과를 함께 보는 것이 좋습니다. 5장 리딩을 추천드립니다.
```

### 10장 추천 키워드

```text
인생, 장기, 미래, 결혼, 사업, 전체운, 큰 고민, 복잡한, 오래된, 방향, 운명, 중요한 선택
```

추천 문구:

```text
이 질문은 단순한 답보다 전체 흐름과 숨겨진 원인을 함께 보는 것이 좋습니다. 10장 켈틱크로스 리딩을 추천드립니다.
```

### 추가 규칙

- 10장 키워드가 1개라도 있으면 10장 그룹에 +1 보정 (긴 고민을 낮은 상품으로 유도하지 않기 위해)
- 질문이 15자 미만이고 물음표로 끝나면 3장 그룹에 +1 보정
- 추천은 **강제가 아니다**. 세 상품 모두 항상 선택 가능하고, 추천 상품에만 `추천` 배지 + 골드 글로우 보더를 표시한다 (code1.html의 `glowing-border` 패턴).
- 3장 리딩의 배열 변형(§6.1)도 이 유틸에서 함께 결정해 반환한다.

**반환 인터페이스** (02~04 문서가 참조할 계약):

```js
analyzeQuestion(question) → {
  recommended: 'tarot3'|'tarot5'|'tarot10',
  reason: '추천 문구 문자열',
  spreadVariant: 'past-present-future'|'me-them-outcome'|'problem-advice-outcome'|'now-obstacle-potential'|null,
  matchedKeywords: string[]
}
```

---

## 8. 카드 선택 UX (핵심 화면)

code3.html + screen3.png를 구현 기준으로 삼는다. 화면은 세로 2분할.

### 8.1 상단 — 스프레드 슬롯

- 선택한 스프레드 배열을 빈 슬롯(점선 보더)으로 표시
- **다음에 채워질 슬롯**은 골드 점선 + 글로우로 강조 (`slot.active`)
- 카드를 선택하면 뒷면 카드가 해당 슬롯으로 이동(트랜지션 0.4s), 슬롯 아래에 위치 의미 라벨 표시 (예: "현재 상황")
- 슬롯 번호는 스프레드 정의 순서를 따른다

### 8.2 하단 — 78장 부채꼴 덱

- 78장의 카드 뒷면을 아치형/부채꼴로 배치 (code3.html의 회전·y오프셋 공식 재사용: `rotate(offset*1.5deg) translateY(|offset|*1.2px)`)
- 좌우 드래그/스와이프로 덱 스크롤
- **중앙에 온 카드만 선택 가능**: 위로 올라오고(-50px) 1.8배 확대 + 골드 글로우 (`focused`)
- 터치하면 상단의 다음 빈 슬롯으로 이동
- 선택된 카드는 덱에서 비활성화 (opacity 0.3 + pointer-events 없음)
- 필요 장수를 모두 채우면 하단에 `선택 완료` 골드 필 버튼 등장 (pulse 애니메이션)

### 8.3 성능 규칙

- 78장을 이미지로 만들지 않는다. 카드 뒷면은 **CSS/SVG 패턴 1개** (진보라 배경 + 골드 라인 문양)를 공유한다 → DOM 78개, 이미지 0장
- 스크롤 포커스 판정은 `scroll` 이벤트 + rAF 스로틀. `getBoundingClientRect` 호출은 프레임당 1회 배치로 제한
- 저사양 기기 대응: `prefers-reduced-motion` 시 부채꼴 대신 가로 스크롤 그리드로 폴백

### 8.4 카드 배정 규칙

- 사용자가 고른 것은 "덱에서의 위치"다. 실제 카드(78장 중 어느 카드인지)는 **선택 완료 시점에 시드 없는 `crypto.getRandomValues` 셔플**로 확정한다 (뽑기 전 카드가 정해져 있으면 안 됨 — 중복 없이)
- **역방향**: 질문형 타로는 카드당 30% 확률로 역방향 허용. 결과 데이터에 `reversed: boolean` 포함

---

## 9. 결과 화면 구조

결과 화면은 아래 순서의 섹션으로 구성한다 (기존 풀이 화면의 `nb-card` 카드 스택 패턴 재사용).

```text
1. 사용자 질문                (인용 스타일로 상단 고정)
2. 선택한 리딩 상품           (3장/5장/10장 + 배열명)
3. 선택된 카드 전체           (앞면 카드 나열, 카드명 + 정/역 표시)
4. 질문 요약                  (질문을 한 문장으로 재정리)
5. 전체 흐름 요약             (카드 전체를 관통하는 3~4문장)
6. 카드별 해석                (위치 의미 × 카드 의미 × 질문 맥락)
7. 카드 조합 해석             (인접/대립 카드 간 상호작용 2~3개)
8. 최종 답변                  (질문에 대한 직접적인 답)
9. 현실적인 조언              (행동 가능한 구체 조언)
10. 주의할 점
11. 다음 행동 가이드          (오늘/이번 주/이번 달 단위)
```

**작성 원칙**: 카드 의미를 단순 나열하지 말 것. 질문과 카드가 연결된 **하나의 상담문**처럼 읽혀야 한다. 모든 문장은 기존 사주 풀이와 같은 평이체.

결과 하단: 공유 버튼(기존 공유 보상 체계 재사용 여부는 추후 결정) + "다른 질문 물어보기" CTA.

---

## 10. 디자인 방향

**Modern Mystical** — 신비롭고 고급스러운, 어두운 배경 + 금색 포인트. 기존 사주앱과 같은 화면 언어를 쓰되 타로에서만 보라 계열 깊이를 더한다.

### 10.1 색상 — 기존 변수 재사용 + 타로 전용 2개 추가

기존 앱 `:root`와 시안 팔레트가 이미 일치하므로 아래만 확정한다.

| 용도 | 값 | 출처 |
|---|---|---|
| 배경 | `var(--bg)` #111415 | 기존 그대로 |
| 카드/패널 | `var(--card)`, 글래스 `rgba(255,255,255,.06)+blur(12px)` | 기존 + DESIGN.md Level 1 |
| 포인트 골드 | `var(--accent-bright)` #d4af37 | 기존 그대로 |
| 보조 텍스트 | `var(--accent)` #d8c4a8 (파치먼트) | 기존 그대로 |
| 본문 텍스트 | `var(--text)` #e1e3e4 | 기존 그대로 |
| **타로 전용 추가** | `--tarot-purple: #2E1A47` (midnight-purple) | DESIGN.md |
| **타로 전용 추가** | `--tarot-blue: #1A237E` (nebula-blue) | DESIGN.md |

> 요청서 초안의 보라 팔레트(#0F0B1F 등)는 참고용으로만 취급하고, 실제 값은 위 표(시안 = 기존 앱 일치 팔레트)로 확정한다.

### 10.2 타이포그래피 — 기존 그대로

- 본문: `'Do Hyeon','Manrope','Noto Sans KR','Apple SD Gothic Neo',sans-serif`
- 제목: `'Do Hyeon','Noto Serif KR','Noto Serif',serif`
- 아이콘: 기존 Material Symbols Outlined
- **EB Garamond·Hanken Grotesk 신규 로드 금지** (폰트 요청 추가 없음)

### 10.3 표면·깊이 (DESIGN.md 규칙 채택)

- 그림자 대신 **글로우**: 활성 요소는 골드 저투명 외곽 글로우 (20~40px, 15% 안팎)
- 글래스 패널: 반투명 배경 + `backdrop-filter: blur(12px)` + 1px 골드 저투명 보더
- 배경: `--bg` 위에 `--tarot-purple` 라디얼 그라디언트를 은은하게 깔아 타로 화면임을 구분
- 카드 모서리: 타로 카드 16px, 일반 컴포넌트 8px
- 버튼: 주버튼 골드 필 + 다크 텍스트 / 보조 버튼 글래스 + 골드 고스트 보더 (기존 `easypay-btn`/`conversion-hook-btn`과 톤 일치)

### 10.4 카드 비주얼

- **뒷면**: 진보라(`--tarot-purple`) 배경 + 골드 라인 문양. CSS 그라디언트/SVG 패턴으로 생성, 이미지 파일 없음
- **앞면**: 초기 버전은 일러스트 없이 구성 — 카드명(세리프 대형) + 대표 심볼(Material Symbol 또는 유니코드) + 골드 프레임 + 정/역 표시. 카드 아트는 추후 이미지 에셋으로 교체 가능한 구조(카드 데이터에 `image` 필드 예약)

---

## 11. 기술 원칙

### 11.1 파일 구조 — 반드시 분리

기존 `index.html`이 이미 거대(약 2만 줄)하므로 타로는 처음부터 별도 파일로 만든다.

```text
tarot/
├ tarot-cards.js        # 78장 카드 데이터 (02에서 정의)
├ tarot-spreads.js      # 스프레드 정의 (배열·위치 의미·레이아웃 좌표)
├ tarot-recommend.js    # 질문 분석·추천 유틸 (§7 인터페이스)
├ tarot-engine.js       # 오늘의 타로 생성기 + 셔플/배정 + 로컬 폴백 리딩 (03에서 정의)
├ tarot-ui.js           # 화면 렌더·카드 선택 UX·애니메이션
└ tarot.css             # 타로 전용 스타일 (전역 오염 금지, .tarot- 프리픽스)
```

`index.html`에는 진입 메뉴 카드와 `<script defer>` 로드, 뷰 컨테이너만 추가한다.

### 11.2 로직 분리

- 오늘의 타로와 질문형 타로 로직을 섞지 않는다 (오늘의 타로는 engine의 독립 함수군)
- 스프레드·카드 데이터는 코드가 아닌 **데이터 파일**로: UI·엔진은 데이터를 읽기만 한다

### 11.3 성능

- 78장 카드를 한 번에 무겁게 렌더링하지 않는다 (§8.3: 공유 CSS 패턴, 이미지 0장, rAF 스로틀)
- 타로 스크립트는 `defer` 로드, 타로 첫 진입 전에는 실행 비용 없음

### 11.4 결제 — mock 우선 구조

- `tarotPay(product) → Promise<{ok, orderId}>` 단일 진입점을 만들고, 초기 구현은 **mock**(확인 모달 → 즉시 성공)으로 한다
- 실제 연결 시 이 함수 내부만 기존 `openPaymentConsent(category,'KAKAOPAY')` 흐름으로 교체
- 카카오페이 연결 시 서버 검증을 위해 `worker/kakao-payment-worker.js`와 `worker/payment-worker.js`의 `CATEGORY_PRICE`에 `tarot3:1500, tarot5:2000, tarot10:3000` 추가 + **워커 재배포 필수**

### 11.5 AI 리딩 + 폴백

- 결과 생성은 워커 엔드포인트 호출(기존 `saju-chat-worker.js` 멀티 프로바이더 패턴 준용, 상세는 `04_ai_reading.md`)
- **API 실패 시**: 카드 데이터의 위치별 해석 템플릿을 조합한 **로컬 기본 결과**를 반드시 보여준다 (§9의 11개 섹션 구조 동일 유지, 기존 앱의 "조용한 폴백" 정책과 동일 — 에러 문구로 신뢰를 깎지 않는다)
- 같은 질문+같은 카드 조합은 결과를 캐시해 재호출하지 않는다

### 11.6 저장 — 유료 결과 재열람

`localStorage` 키 규약 (기존 `saju_*` 패턴을 따름):

```text
tarot_daily_v1:{userId}:{date}     # 오늘의 타로 당일 캐시
tarot_reading_v1:{userId}          # 질문형 결과 배열 (최근 10개, 각 항목에
                                   # {orderId, product, question, cards[], resultHtml, createdAt})
tarot_pending_v1                   # 결제 왕복 중 상태 백업 (session+local 이중)
```

- 결과 목록 화면("내 리딩 보관함")에서 과거 결과 재열람 가능. 보관 30일.

### 11.7 i18n·접근성

- 정적 UI 문구는 기존 `STATIC_I18N`에 등록 (en·zh·ja), 리딩 본문은 기존 번역 파이프라인 대상
- 카드 선택은 터치 외 키보드(←→ 이동, Enter 선택)로도 가능해야 함
- `prefers-reduced-motion` 폴백 (§8.3)

---

## 12. 개발 산출물 로드맵

이 문서를 기준으로 아래 파일을 순서대로 작성한다.

```text
01_project.md          ← 이 문서. 전체 설계·계약의 기준
02_tarot_database.md   # 78장 카드 데이터 스키마 + 전체 데이터
                       #   {id, name, nameKo, arcana, suit, number, keywords[],
                       #    upright:{core, love, money, work, advice, caution},
                       #    reversed:{...}, symbol, image(예약)}
                       # + 스프레드 정의 데이터 (위치 의미·레이아웃 좌표)
03_tarot_engine.md     # 오늘의 타로 시드 생성기 · 셔플/배정 · 배열 변형 결정
                       # · 로컬 폴백 리딩 조립기 (§4.3, §7, §8.4, §11.5 구현 명세)
04_ai_reading.md       # AI 리딩 워커 설계 — 프롬프트 구조, §9 섹션별 출력 규격,
                       #   프로바이더 폴백 체인, 캐시, 요금 가드
05_webapp.md           # 화면별 구현 명세 — 뷰 전환, 4개 화면 상세,
                       #   기존 index.html 통합 지점, tarot.css 컴포넌트 목록
06_prompt_examples.md  # 질문 유형별 프롬프트·응답 예시 (연애/재회/이직/금전/인생)
```

### 구현 순서 권장

1. `02` 카드·스프레드 데이터 → 2. 오늘의 타로 (무료, 로컬 완결 — 가장 먼저 배포 가능) → 3. 질문 입력·추천·mock 결제 → 4. 카드 선택 UX → 5. 로컬 폴백 결과 → 6. AI 워커 연결 → 7. 실결제 연결·워커 재배포

---

## 부록 A. 확정된 결정 사항 요약

| 항목 | 결정 |
|---|---|
| 폰트 | 기존 앱 그대로 (신규 폰트 로드 금지) |
| 색상 | 기존 `:root` 변수 + `--tarot-purple`/`--tarot-blue` 2개 추가 |
| 가격 | 3장 1,500 / 5장 2,000 / 10장 3,000원 |
| 기본 추천 상품 | 5장 (분석 동점·무매칭 시) |
| 역방향 | 질문형만, 카드당 30% / 오늘의 타로는 정방향만 |
| 카드 아트 | 초기엔 CSS/SVG 구성, `image` 필드로 추후 교체 가능 |
| 오늘의 타로 시드 | `hash(날짜+userId)` — 같은 날 같은 결과 |
| 결제 | mock 먼저, 진입점 함수 1개로 캡슐화 |
| AI 실패 | 로컬 템플릿 폴백, 에러 문구 비노출 |
| 저장 | localStorage, 질문형 결과 30일·10개 보관 |
| 파일 | `tarot/` 디렉터리 분리, `.tarot-` CSS 프리픽스 |
